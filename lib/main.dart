import 'package:flutter/material.dart';
import 'package:flutter_flavour/my_app.dart';

void main() {
  runApp(const MyApp(type: "Default Flavor",));
}


// Flutter run and build commands come with a –flavor flag.
// So, from the command line, you can simply type flutter run --flavor dev  and flutter fun --flavor prod 
